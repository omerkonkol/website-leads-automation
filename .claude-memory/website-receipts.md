---
name: website-receipts
description: "איפה נשמרות קבלות על אתרים שהמשתמש בנה, ושיטת השיוך ללקוח"
metadata: 
  node_type: memory
  type: project
  originSessionId: 3139589f-6ade-4e2b-af95-4a1b07583e55
---

המשתמש הוא עוסק פטור (מספר 213712698) ומוציא קבלות על אתרים שהוא בונה דרך תוכנת חשבונות (איזיקאונט/morning), בלי מע"מ.

הקבלות שמורות בתיקייה `e:\system\קבלות-לאתרים\`:
- כל קבלה: קובץ PDF בשם `<לקוח>_<טלפון>_<סכום>ils_<תאריך>_<מספר-קבלה>.pdf`
- קובץ `index.md` בתיקייה — טבלה שמרכזת: מספר קבלה, לקוח, טלפון, אתר, סכום, תאריך, קישור לקובץ

כשהמשתמש מוריד קבלה חדשה ומבקש לשמור — מעתיקים אותה לתיקייה עם השם הזה ומוסיפים שורה ל-index.md.

קבלות מקור יורדות בד"כ ל-`E:\הורדות\`.

**מספור קבלות — חשוב:** הספרור חייב להיות רציף ויחיד בלי קשר לאפליקציית הקבלות (המשתמש מתחלף בין אפליקציות בגלל הגבלת 10 קבלות חינם בחודש). תמיד לחשב את המספר הבא = (המקסימום הקיים ב-index.md) + 1, ולומר למשתמש מראש את מספר הקבלה הבא שעליו להזין באפליקציה החדשה.

קבלות עד כה: 80001 gscocktails 575₪ (16/06, bit), 80002 איציק וינשטיין 580₪ (17/06, bit), 80003 עמית אבוחצירה 540₪ (17/06, bit), 80004 עמית אבוחצירה 690₪ "דף נחיתה + דומיין" (17/06) — ייתכן כפילות עם 80003 (לבטל אחת), 80005 בטי כודדה (Betty's fly) 790₪ דף נחיתה (22/06, העברה בנקאית), 80006 עליזה גולדברג 1250₪ דף נחיתה (23/06, ביט — הופק ב-SUMIT, עבר ממורנינג בגלל מגבלת 10 חינם). **הקבלה הבאה: 80007.** מספרים מבוטלים נשארים תפוסים — לא ממחזרים. סך הכנסות במערכת nova-crm: 3,885₪.

עקרון חשבונאי שהובהר למשתמש: קבלה = כסף שהתקבל בפועל, לא סכום ההזמנה. אם הזמנה גדלה — קבלה נוספת על ההפרש בלבד, לא קבלה חדשה על הכל. טעות מבטלים (לא מוחקים). סך קבלות תקפות = סך הכסף שהתקבל.

הקבלות מוטמעות ב-nova-crm דרך seed.sql — ראה [[nova-crm-project]].
